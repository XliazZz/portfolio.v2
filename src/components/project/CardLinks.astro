---
import Button from "../base/Button.astro";

interface Props {
  demoUrl?: string;
  githubUrl?: string;
}

const { demoUrl, githubUrl } = Astro.props;
---

<div
  class={`flex justify-center items-center
		w-full px-1
		font-heading text-sm mt-2
		rounded gap-3 uppercase lg:px-0 lg:py-0`}
>
  {
    demoUrl && (
      <Button
        icon="external-link"
        href={demoUrl}
        class={`py-0 text-sm w-full xl:py-3.5 rounded-lg`}
        ariaLabel="Ver Demo del proyecto en una nueva pestaña"
      >
        Ver Demo
      </Button>
    )
  }
  {
    githubUrl && (
      <Button
        icon="fa:github"
        href={githubUrl}
        class={`py-0 text-sm w-full xl:py-3.5 rounded-lg 
					hidden md:flex`}
        variant="secondary"
        ariaLabel="Ver Código Fuente en GitHub (nueva pestaña)"
      >
        Código
      </Button>
    )
  }
</div>
