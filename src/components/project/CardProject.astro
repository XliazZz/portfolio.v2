---
import TechChips from "./TechChips.astro";
import CardLinks from "./CardLinks.astro";
import Badge from "../ui/Badge.astro";

interface Props {
  title: string;
  demoUrl?: string;
  githubUrl?: string;
  technologies: Array<{ name: string; icon: string }>;
  featuredImage: string;
  description: string;
  rol: string;
  category: string;
}

const {
  title,
  demoUrl,
  githubUrl,
  technologies,
  featuredImage,
  description,
  rol,
  category,
} = Astro.props;
---

<div
  class="flex flex-col overflow-hidden
      w-full h-full max-w-[26rem]
      m-0
      bg-white
      rounded-xl border border-gray-200
      shadow-sm transition-all
      justify-between hover:shadow-md duration-300 hover:border-indigo-100
      xl:m-4"
>
  <div class="h-max
      border-b border-gray-100
      relative group">
    <img
      class="object-cover
        w-full h-48
        rounded-t-xl
        transition-transform
        group-hover:scale-105 duration-300"
      src={featuredImage}
      alt={`Captura del proyecto ${title}`}
      width={400}
      height={200}
    />

    <div
      class="bg-indigo-600
        rounded-t-xl
        opacity-0 transition-opacity
        absolute inset-0 group-hover:opacity-5 duration-300"
    >
    </div>
  </div>

  <div
    class="flex-grow flex flex-col
      w-full
      px-4
      rounded
      md:pt-3
      lg:pt-2"
  >
    <div
      class="flex
        w-full
        mb-4
        items-start justify-between gap-0
        md:mb-2
        lg:gap-2"
    >
      <h6
        class="mx-auto
          font-sans font-bold text-lg text-gray-800 font-heading text-center
          antialiased
          sm:py-1
          md:m-0 md:mt-0
          lg:mx-0 lg:mt-0 lg:text-left lg:text-base"
      >
        {title}
      </h6>

      <div class="flex flex-shrink-0
          my-auto
          gap-2">
        <Badge
          text={rol}
          type="role"
          roleType={rol as "Full-Stack" | "Front-End" | "Back-End"}
        />

        <Badge text={category} type="category" />
      </div>
    </div>

    <p
      class="mb-4
        font-body text-sm text-gray-600 leading-relaxed
        antialiased line-clamp-3 ocultar-en-movil
        md:text-base"
    >
      {description}
    </p>

    <div class="lg:mt-auto">
      <TechChips technologies={technologies} />
    </div>
  </div>

  <div class="px-4 pb-4
      md:mt-3
      lg:mt-3.5">
    <CardLinks {...{ demoUrl, githubUrl }} />
  </div>
</div>
<style>
  @media (max-width: 768px) {
    .ocultar-en-movil {
      display: none;
    }
  }
</style>
