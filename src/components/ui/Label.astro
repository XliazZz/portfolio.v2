---
interface Props {
  for: string;
  text: string;
}

const { for: forId, text } = Astro.props;
---

<label
  for={forId}
  class="block text-sm font-semibold leading-6 text-indigo-900 font-heading mb-2"
>
  {text}
</label>
